{
  "name": "smart-mobile-pro",
  "version": "1.0.0",
  "scripts": {
    "build": "cd smartmobile && react-scripts build",
    "ionic:build": "cd smartmobile && react-scripts build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "web-vitals": "^5.1.0"
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
  }
}
